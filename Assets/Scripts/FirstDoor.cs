using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FirstDoor : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //Make this pop up the puzzle, in this case just the button. Once the button has been pushed, play a sound (any sound) to signalise that the door is open and
        //that now clicking the door should lead to the next scene/room.
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
