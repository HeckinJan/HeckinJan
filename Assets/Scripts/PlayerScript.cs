using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerScript : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //This script is for the dialogue boxes to pop up and also to keep track of the players progression. For now, room 1, it doesn't need to do anything.
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
